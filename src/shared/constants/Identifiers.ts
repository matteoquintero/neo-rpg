/**
 * Collection of prefix identifiers used throughout the application
 * for consistent naming conventions
 */
export const Identifiers = {
  JOB: 'Job',
  ENTITY_TYPE: 'EntityType',
  STATE: 'State',
  NAME: 'Name',
  CHARACTER: 'Character'
} as const;

export const JOB_PREFIX = Identifiers.JOB;
export const ENTITY_TYPE_PREFIX = Identifiers.ENTITY_TYPE;
export const STATE_PREFIX = Identifiers.STATE;
export const NAME_PREFIX = Identifiers.NAME;
export const CHARACTER_PREFIX = Identifiers.CHARACTER;
